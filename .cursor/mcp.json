{
  "$comment": "Cursor MCP config — same server set as .mcp.json (Claude Code). Validated packages. See docs/agent-tooling.md for env setup (magic/github).",
  "mcpServers": {
    "context7": { "command": "npx", "args": ["-y", "@upstash/context7-mcp@latest"] },
    "playwright": { "command": "npx", "args": ["-y", "@playwright/mcp@latest"] },
    "sequential-thinking": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-sequential-thinking@latest"]
    },
    "magic": { "command": "npx", "args": ["-y", "@21st-dev/magic@latest"] },
    "github": { "command": "npx", "args": ["-y", "@modelcontextprotocol/server-github@latest"] }
  }
}
